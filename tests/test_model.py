"""
模型单元测试

==================== 给初学者的整体说明 ====================

【模型代码为什么要测试？测什么？】
模型写错一个维度、漏一个掩码，程序往往【不报错】，loss 也照样下降
（只是学歪了）——这类 bug 最难发现。所以模型测试的核心思路是
"用数学性质去卡"，而不是只看"能不能跑"。

【计划覆盖的 4 类测试】（按重要性排序）

1. 因果性测试（最重要，GPT 的命门）
   构造一个序列，只改动第 t 个位置之后的 token，
   断言前 t 个位置的 logits 完全不变。
   如果变了，说明未来信息泄漏到了过去——掩码写错了，
   模型在"开卷考试"，生成的每个字都不可信。

2. 形状测试
   输入 (B, T) 的 ids，断言输出 logits 是 (B, T, vocab_size)；
   再试几个不同的 B、T，确认没有写死维度。

3. 过拟合测试（最能验证前向/反向整体正确性）
   用同一个 batch 反复训练几百步（不看 val），
   断言 loss 降到接近 0。
   一个实现正确的模型必然能把一小段数据背下来；
   背不下来 = 前向、反向、loss 三者中至少一个有问题。

4. KV cache 一致性测试（sample.py 写完后补）
   开关 cache 各生成一遍，固定种子下断言输出逐 token 相同。

【和 test_tokenizer.py 相同的约定】
  - 每个测试函数以 test_ 开头，pytest 自动发现
  - 用小配置（dim=32、2 层、seq_len=16）让测试秒级跑完
  - 浮点比较用 torch.allclose，不要用 ==

依赖：torch、pytest、src.model
"""

# TODO: model.py 实现后按上面的清单补测试
